#include <iostream>
using namespace std;

int main() 
{
	cin.tie(0);
	ios::sync_with_stdio(0);
	int r;
	cin>>r;
	int l = r * 2 * 2;
	cout<<"A";
	for(int i = 0; i < l; i++)
	{
		cout<<"a";
	}
	cout<<"h"<<'\n';
	return 0;
}
